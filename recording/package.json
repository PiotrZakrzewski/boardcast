{
  "name": "recording",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "record:lancer": "node record-lancer.js",
    "record:lancer:test": "playwright test tests/record-lancer.spec.ts --headed",
    "record:standalone": "node serve-and-record.js",
    "serve:demo": "node -e \"const express = require('express'); const app = express(); app.use(express.static('.')); app.use('/dist', express.static('../dist')); app.listen(3001, () => console.log('Demo at http://localhost:3001/lancer-demo.html'));\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@playwright/test": "^1.54.2"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
