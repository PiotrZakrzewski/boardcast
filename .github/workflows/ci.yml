name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [20, 22]

    steps:
    - name: Checkout repository
      uses: actions/checkout@v5

    - name: Setup Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'

    - name: Install dependencies
      run: cd boardcast && npm install

    - name: Run type checking
      run: cd boardcast && npm run typecheck

    - name: Run tests
      run: cd boardcast && npm run test

    - name: Build packages
      run: cd boardcast && npm run build:all